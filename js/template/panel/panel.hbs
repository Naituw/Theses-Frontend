            <div class="panel">
                <header> 
                    <section class="custom"> 
                        <div class="info"> 
                            <h1>{{name}}</h1> 
                            <h2>{{desc}}</h2> 
                        </div> 
                    </section> 
                    <nav> 
                        <a class="close" {{action closePanel href=true}}>Close</a> 
                    </nav> 
                </header> 
                <section class="content">
                     {{outlet content}}
                </section>
            </div>